# INVALID: Parameter 'Authorization' uses 'in: body' which is NOT valid in
# OpenAPI 3.0. Valid values for 'in' are: query, header, path, cookie.
# In OpenAPI 3.0 request bodies must use 'requestBody', not body parameters.
openapi: 3.0.3
info:
  title: GoRest - Create Todo (Bearer Token - Invalid)
  version: 1.0.0
servers:
  - url: https://gorest.co.in/public/v2
paths:
  /todos:
    post:
      summary: Create a todo
      operationId: createTodo
      parameters:
        - name: Authorization
          in: body            # INVALID: 'body' is not a valid 'in' value in OpenAPI 3.0
          required: true
          schema: { type: string }
      responses:
        "201":
          description: Todo created
        "401":
          description: Unauthorized
