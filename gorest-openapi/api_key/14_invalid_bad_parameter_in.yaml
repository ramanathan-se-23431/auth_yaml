# INVALID: Parameter uses 'in: body' which is NOT valid in OpenAPI 3.0.
# Valid values for 'in' are: query, header, path, cookie only.
# Request bodies in OpenAPI 3.0 must use the 'requestBody' field, not body parameters.
openapi: 3.0.3
info:
  title: GoRest - Create Todo (API Key - Invalid)
  description: Invalid spec - uses 'in: body' which is not allowed in OpenAPI 3.0.
  version: 1.0.0
servers:
- url: https://gorest.co.in/public/v2
security:
- apiKeyAuth: []
paths:
  /todos:
    post:
      summary: Create a new todo
      description: Create a new todo record in GoRest.
      operationId: createTodo
      parameters:
      - name: X-API-Key
        in: body           # INVALID: 'body' is not a valid 'in' value in OpenAPI 3.0
        description: API Key for authentication
        required: true
        schema:
          type: string
      responses:
        "201":
          description: Todo created successfully
        "401":
          description: Unauthorized
components:
  securitySchemes:
    apiKeyAuth:
      type: apiKey
      in: header
      name: X-API-Key
