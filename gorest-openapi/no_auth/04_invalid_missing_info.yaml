# INVALID: The required 'info' object is completely missing.
# OpenAPI 3.0 spec requires 'info' with 'title' and 'version'.
openapi: 3.0.3
servers:
- url: https://gorest.co.in/public/v2
paths:
  /comments:
    get:
      summary: List all comments
      description: Retrieve a list of all comments from GoRest.
      operationId: listComments
      parameters:
      - name: page
        in: query
        description: Page number for pagination
        required: false
        schema:
          type: integer
      responses:
        "200":
          description: A list of comments retrieved successfully
